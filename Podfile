# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def config
  pod 'Alamofire', '~> 4.4'
  pod 'Locksmith', '~> 3.0'

  # Pods for Gameventory
end

target 'Gameventory' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  config
end

target 'Gameventory-local' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  config
end

target 'GameventoryTests' do
  inherit! :search_paths
  # Pods for testing
end

target 'GameventoryUITests' do
  inherit! :search_paths
  # Pods for testing
end
